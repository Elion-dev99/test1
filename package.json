{
  "name": "mmorpg-boss-notifier",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply boss-notifier-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply boss-notifier-db --remote",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260831.1",
    "typescript": "^5.7.3",
    "wrangler": "^4.4.0"
  }
}
